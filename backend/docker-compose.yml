services:
  backend:
    # Docker Hub: <username>/termul-backend:latest
    # Custom registry: <registry-host>/<username>/termul-backend:latest
    image: ${DOCKER_IMAGE}
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
